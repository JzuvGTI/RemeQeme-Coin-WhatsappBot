const {
  formatCoins,
  buildPlayerProfileText,
  buildRegisterText,
  buildAddCoinText
} = require("./reply-builder");

module.exports = {
  formatCoins,
  buildPlayerProfileText,
  buildRegisterText,
  buildAddCoinText
};
